# Built-in default official release archives
#
# Place official Apache tar.gz files here (not committed to git — large binaries):
#   apache-dolphinscheduler-3.2.2-bin.tar.gz
#   apache-seatunnel-2.3.11-bin.tar.gz
#
# Download:
#   .\scripts\download-bundled.ps1
#
# On startup, integration-server extracts these into runtime/{component}/{version}/
